# إعداد وتشغيل ملاحظات 5S

تمت إضافة صفحة **ملاحظات 5S** إلى التطبيق دون تعديل وظائف الشاشات الحالية. الصفحة تحفظ كل يوم بشكل مستقل، وتربط كل ملاحظة بالقسم والمكان، وتدعم صورة الملاحظة وصورة الفعل التصحيحي والوصف والتحليل.

## التخزين المجاني

بيانات الأماكن تحفظ في `5s_settings/locations`، وبيانات الملاحظات اليومية تحفظ في مجموعة `5s_notes`. كل ملاحظة تحمل `date` و`monthKey`، بينما الصور الجديدة لا تستخدم Firebase Storage؛ يتم ضغطها إلى WebP داخل المتصفح ثم إرسالها إلى رابط Google Apps Script الموجود في `CONFIG.GOOGLE_API_URL`. التطبيق يحفظ في Firestore رابط الصورة ومعرّفها فقط، ولا يضع بيانات الصورة داخل مستند Firestore.

يجب أن يكون Google Apps Script منشوراً كـ Web App مع صلاحية التنفيذ كمالك السكربت، وأن يقبل طلب `IMAGE_UPLOAD` بالحقول `filename` و`mimeType` و`base64` و`date`. التطبيق يرسل كذلك `source: "5S"` و`folderPath` لمساعدة السكربت على تنظيم الصور في Google Drive حسب الشهر واليوم. نقطة الرفع الحالية يعاد استخدامها من نظام العيوب الموجود بالفعل.

## الأرشيف الشهري المجاني

بما أن المشروع على خطة Firebase Spark، لا يعتمد هذا الإصدار على Cloud Functions أو Cloud Scheduler. عند فتح التطبيق في شهر جديد، ينفذ أول مستخدم يفتح التطبيق عملية أرشفة آمنة من المتصفح: يجمع أعداد الملاحظات والأفعال التصحيحية حسب الشهر والقسم والمكان، يحفظ الملخص داخل `5s_monthly_summaries/{monthKey}`، ثم يحذف مستندات الملاحظات القديمة من Firestore. الصور التي تم رفعها إلى Google Drive تبقى في Drive كمرجع؛ حذفها تلقائياً من Drive يحتاج أن يدعم Google Apps Script عملية حذف مصرحاً بها، ولم يتم حذف أي ملف Drive تلقائياً حتى لا تضيع الصور دون مسار استرجاع.

الملخصات الشهرية تظل ظاهرة في صفحة 5S بعد حذف السجلات اليومية، وتحتوي على إجمالي الملاحظات، وعدد الأفعال التصحيحية، ونسبة الإغلاق، والتفاصيل حسب كل مكان.

## التفعيل المطلوب في Google Drive

يجب التأكد من أن رابط Google Apps Script في `app.js` هو رابط Web App صالح، وأن السكربت يكتب في مجلد Google Drive المقصود. لا يحتاج المستخدم إلى إدخال Service Account أو ترقية Firebase لهذا المسار. إذا كان السكربت الحالي يرفع الصور في مجلد واحد فقط، يمكن تعديل كود Apps Script ليستخدم `folderPath` وينشئ مجلدات `5S/{الشهر}/{اليوم}`.

## ملاحظة أمان

لا تضع مفاتيح Service Account داخل الواجهة أو مستودع GitHub. قواعد Firestore الحالية لم يتم تغييرها حتى لا تتأثر الشاشات الموجودة؛ يجب مراجعة قواعد القراءة والكتابة الخاصة بـ `5s_notes` و`5s_monthly_summaries` بما يتوافق مع نظام الدخول المعتمد في المؤسسة.

## مسار Firebase Functions الاختياري

يوجد كود اختياري في `functions/index.js` للأرشفة عبر Cloud Functions، لكنه غير مطلوب للمسار المجاني الحالي ولن يعمل على خطة Spark بدون تفعيل الفوترة. المسار المستخدم حالياً هو Google Drive للصور والأرشفة من المتصفح.
