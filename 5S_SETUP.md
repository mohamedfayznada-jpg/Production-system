# إعداد وتشغيل ملاحظات 5S

تمت إضافة صفحة **ملاحظات 5S** إلى التطبيق دون تعديل وظائف الشاشات الحالية. الصفحة تحفظ كل يوم بشكل مستقل، وتربط كل ملاحظة بالقسم والمكان، وتدعم صورة الملاحظة وصورة الفعل التصحيحي والوصف والتحليل.

## التخزين

- بيانات الأماكن تحفظ في `5s_settings/locations`.
- بيانات الملاحظات اليومية تحفظ في مجموعة `5s_notes`، وكل ملاحظة تحمل `date` و`monthKey`.
- الصور تحفظ في Firebase Storage داخل مسار `5s/{date}/{noteId}/`، مع ضغطها إلى WebP قبل الرفع.
- الصور لا تدخل إلى مستند Firestore نفسه؛ المستند يحفظ المسار والرابط فقط.

## الأرشيف الشهري

الملف `functions/index.js` يحتوي على وظيفة مجدولة باسم `archive5SNotes` تعمل في أول يوم من كل شهر الساعة 00:15 بتوقيت القاهرة. الوظيفة تجمع عدد الملاحظات وعدد الأفعال التصحيحية لكل قسم ومكان، تحفظ النتائج داخل `5s_monthly_summaries/{monthKey}`، ثم تحذف الصور وبيانات الملاحظات اليومية القديمة. لذلك تبقى الأرقام والتحليلات الشهرية كشاهد على التحسينات، بينما لا تبقى الصور بعد الأرشفة.

## التفعيل مرة واحدة

من داخل مجلد المشروع، وبعد تسجيل الدخول إلى حساب Firebase الصحيح، يتم تنفيذ:

```bash
cd /home/ubuntu/Production-system
cd functions && npm install
cd ..
firebase deploy --only functions:archive5SNotes
```

يتطلب تشغيل الوظيفة المجدولة تفعيل Cloud Scheduler في مشروع Firebase، وقد يتطلب ذلك خطة فوترة مناسبة من Google Cloud. لم يتم تنفيذ النشر تلقائياً من بيئة التطوير، حتى لا يتم إجراء تغيير خارجي على مشروع Firebase دون موافقة مالك الحساب.

## ملاحظة أمان مهمة

قبل تفعيل الاستخدام الفعلي، يجب مراجعة قواعد Firestore وFirebase Storage في مشروع `production-b0b2c` بحيث تسمح للتطبيق الحالي بالقراءة والكتابة المطلوبة، مع تقييد مسارات `5s/` حسب نظام الدخول المعتمد في المؤسسة. لم يتم تغيير قواعد Firebase الحالية حتى لا تتأثر الشاشات الموجودة.
